Based on the provided code, the following Python packages need to be installed:

```
Flask==2.2.2
Flask-SQLAlchemy==2.5.1
Werkzeug==2.2.2
```

You can install these packages using pip:

```bash
pip install Flask==2.2.2 Flask-SQLAlchemy==2.5.1 Werkzeug==2.2.2
```

Or, you can create a `requirements.txt` file with the following content:

```
Flask==2.2.2
Flask-SQLAlchemy==2.5.1
Werkzeug==2.2.2
```

Then, you can install the packages using pip:

```bash
pip install -r requirements.txt
```

Note: The versions of the packages are specified to ensure compatibility, but you can use the latest versions if you prefer. However, be aware that using the latest versions may introduce compatibility issues if the code is not compatible with the latest versions of the packages. 

Here is the `requirements.txt` file:

```
# requirements.txt
Flask==2.2.2
Flask-SQLAlchemy==2.5.1
Werkzeug==2.2.2
```